{% extends 'base.html.twig' %}

{% block title %}Steph{% endblock %}

{% block body %}
    <h1>Steph</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ steph.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ steph.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ steph.age }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('steph_index') }}">back to list</a>

    <a href="{{ path('steph_edit', {'id': steph.id}) }}">edit</a>

    {{ include('steph/_delete_form.html.twig') }}
{% endblock %}
